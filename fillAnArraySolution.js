function arr(n) {
  var newArr = [];      //set new array
  for (var i = 0; i < n; i++) {     //loop as long as number is less than N
    newArr.push(i);     //push the new number into the new array
  }
  return newArr;
}
